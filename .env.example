# Local development: start FastAPI with
# `cd isl-model; python -m uvicorn api.main:app --reload --port 8000`.
# For the hosted website, replace this with the public HTTPS URL of that service.
EXPO_PUBLIC_ISL_API_URL=http://localhost:8000
